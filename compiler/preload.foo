
#define QUOTE(...) #__VA_ARGS__

u8 *preload_code_text = QUOTE(

enum Type_Kind {
    // 0 is unused in valid programs
    VOID = 1,
    BOOL = 2,
    // 3 is unused in valid programs
    U8  = 4,
    U16 = 5,
    U32 = 6,
    U64 = 7,
    I8  = 8,
    I16 = 9,
    I32 = 10,
    I64 = 11,
    POINTER = 12,
    ARRAY = 13,
    // 14 is unused in valid programs
    STRUCT = 15,
    ENUM = 16,
}

);
